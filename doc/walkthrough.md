# Walkthrough

## 2026-03-10 第二轮调整计划

### 目标

- 精简菜品管理页面，移除新模式无用字段。
- 导入婚宴 8 个 + 团年 5 个真实套餐模板。
- 让菜单编辑器支持规格切换。
- 在套餐卡片补上删除入口。

### 关键决策

- 套餐内容不走 OCR，改用 `codex exec -i <image>` 子代理读取 PDF 转出的高分辨率页面图片。
- 团年 PDF 首页是 3 个盆菜，不在本轮 13 个套餐范围内。
- 团年页实际价格显示为 `财运亨通宴 1688`，后续导入按图片真实价格写入。

### 执行记录

#### Step 1: 精简菜品管理页面

- 移除菜品列表中的上菜单位、招牌菜、必点菜、状态列。
- 菜名列不再展示下架/招牌/必点标签。
- 编辑弹窗只保留菜名、分类、售价、成本、价格描述。
- 新增菜品 payload 不再传 `serving_unit`、`serving_split`、`is_signature`、`is_must_order`。

#### Step 2: 导入套餐模板

- 使用 `codex exec -i` 子代理对 4 页婚宴 PDF + 1 页团年 PDF 做视觉提取，并再跑一次多图复核。
- 团年 PDF 首页 3 个盆菜不纳入本轮导入；本轮只写入 8 个婚宴套餐 + 5 个团年套餐。
- 新增 `backend/scripts/import_packages.py`，按分组名幂等导入 `婚宴套餐` 与 `团年套餐`。
- `find_or_create_dish()` 先做菜名精确匹配，未命中时自动补建 0 元菜品并按关键词推断分类。
- 团年页实际使用 `财运亨通宴 1688`，而不是计划稿里的 `1288`。

#### Step 3: 菜单编辑器规格切换

- 待执行。

#### Step 4: 套餐删除按钮

- 待执行。
