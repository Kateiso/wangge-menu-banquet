name: wangge

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend:/app/backend
      - db-data:/app/data
    restart: unless-stopped

volumes:
  db-data:
